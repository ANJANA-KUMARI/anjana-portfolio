{
  "name": "anjana-portfolio",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "check": "npm run build --workspace client && npm run check --workspace server"
  },
  "devDependencies": {
    "concurrently": "9.2.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
